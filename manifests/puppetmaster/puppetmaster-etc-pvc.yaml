apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: puppetmaster
    enviroment: production
  name: puppetmaster-etc
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 50Mi
