apiVersion: v1
kind: ReplicationController
metadata:
  labels:
    app: nfs-server
    enviroment: development
  name: nfs-server
spec:
  replicas: 1
  selector:
    app: nfs-server
  template:
    metadata:
      labels:
        app: nfs-server
        enviroment: development
    spec:
      containers:
      - image: gcr.io/google_containers/volume-nfs
        name: nfs-server
        ports:
        - containerPort: 2049
          name: nfs-server
        securityContext:
          privileged: true
