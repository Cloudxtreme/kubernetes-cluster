apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: jenkins
    enviroment: production
  name: jenkins-slave-java
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1000Mi
