apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: jenkins
    enviroment: production
  name: jenkins-slave-docker
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1000Mi
