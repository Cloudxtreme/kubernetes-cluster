apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: jenkins
    enviroment: production
  name: jenkins-slave-golang
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1000Mi
