apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aptly-pool
  labels:
    app: aptly
    enviroment: production
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2000Mi
