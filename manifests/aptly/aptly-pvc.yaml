apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aptly
  labels:
    app: aptly
    enviroment: production
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 8000Mi
