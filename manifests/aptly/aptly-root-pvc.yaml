apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aptly-root
  labels:
    app: aptly
    enviroment: production
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
