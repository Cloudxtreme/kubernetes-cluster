apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: aptly-public
  labels:
    app: aptly
    enviroment: production
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 4000Mi
