apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: aptly
    enviroment: production
  name: aptly-public
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 4000Mi
