apiVersion: v1
kind: Service
metadata:
  labels:
    app: confluence
    enviroment: production
  name: confluence-postgres
spec:
  ports:
  - name: postgres
    port: 5432
  selector:
    app: confluence
    role: postgres
