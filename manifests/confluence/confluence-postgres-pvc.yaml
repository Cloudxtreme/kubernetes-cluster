apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: confluence
    enviroment: production
  name: confluence-postgres
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 500Mi
