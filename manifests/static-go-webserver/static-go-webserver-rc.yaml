apiVersion: v1
kind: ReplicationController
metadata:
  labels:
    app: static-go-webserver
    enviroment: development
  name: static-go-webserver
spec:
  replicas: 2
  selector:
    app: static-go-webserver
  template:
    metadata:
      labels:
        app: static-go-webserver
        enviroment: development
    spec:
      containers:
      - image: bborbe/static-go-webserver:latest
        name: static-go-webserver
        ports:
        - containerPort: 80
          name: static-go-webserver
